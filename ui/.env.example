# Origin of the Momentum API. The value below is the Spring Boot default.
# The client calls this cross-origin, so the dev server origin
# (http://localhost:5173) must be listed in the API's CORS_ALLOWED_ORIGINS.
VITE_API_BASE_URL=http://localhost:8080
